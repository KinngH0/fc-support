services:
  - type: web
    name: fc-support
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FC_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.9.0
    plan: free 